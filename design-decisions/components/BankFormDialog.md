# BankFormDialog

| שדה | ערך |
|-----|-----|
| Figma | הוספת בנק |
| סטטוס | רכיב דף חדש + שימוש ב-AllocatedConferencesSection משותף |

## 2026-06-10

מודל הוספת בנק — כמו מבצע ללא שדות תאריך:

- רוחב 900px כמו מבצע; גובה לפי תוכן (ללא `645px` קבוע — אין שורת תאריכים)
- שם הבנק*, תיאור
- ועידות בבנק (N) — חיפוש, הוסף, טבלה
- כפתורי שמור / ביטול בצד שמאל

קבצים: `BankFormDialog.tsx`, `AllocatedConferencesSection.tsx`.
